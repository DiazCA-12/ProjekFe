import { authActionTypes } from "./useractiontype";
import { productActionTypes } from "./productactiontype";
import { purchasesActionTypes } from "./purchasesactiontype";

export const actionTypes = {
    ...authActionTypes,
    
}

export const actionTypesProduct = {
    ...productActionTypes,
    
}

export const actionTypesPurchases = {
    ...purchasesActionTypes,
    
}